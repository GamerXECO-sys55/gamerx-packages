# GamerX OS earlyoom configuration
# Trigger earlier than upstream defaults; preserve the user's shell and DE.
EARLYOOM_ARGS="-r 60 -m 8 -s 4 --avoid '^(systemd|sshd|hyprland|Hyprland|sddm|gnome-shell|kwin_wayland)$' --prefer '^(chrome|chromium|firefox|electron|node)$'"
